name: Build + Run
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    container: zleba/rooteigen:latest
    steps:
      - name: Compile
        uses: actions/checkout@v3
      - run: make
      - name: Run
        uses: actions/checkout@v3
      - run: ./runBS
